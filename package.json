{
  "name": "resell-tracker-extension",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node build.js",
    "build:firefox": "node build.js --firefox",
    "build:all": "node build.js && node build.js --firefox",
    "package:firefox": "node build.js --firefox && cd dist-firefox && zip -r ../resell-tracker-sync-firefox.zip .",
    "watch": "node build.js --watch",
    "watch:firefox": "node build.js --firefox --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "@types/webextension-polyfill": "^0.12.5",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "webextension-polyfill": "^0.12.0"
  }
}
